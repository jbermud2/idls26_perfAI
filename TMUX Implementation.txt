TMUX Implementation

cd /jet/home/<username>
tmux new -s perfai

interact -p GPU-shared --gres=gpu:v100-32:1 -t 8:00:00 -A <project allocation>

module load anaconda
conda deactivate
conda activate <env name>

*run your code*

To safely detach (run in the background):
ctrl + b and d

To reattach Later:
ssh <GPU node>
tmux attach -t perfai

To kill current session:
exit
